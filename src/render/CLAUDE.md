# Render Directory

This directory contains rendering logic for the statusline output.

## Files

- `index.ts` - Main render coordinator that determines what to display
- `prayer-line.ts` - Formats prayer time display with countdown and colors
- `colors.ts` - ANSI color escape code helpers
